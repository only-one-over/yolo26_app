# Core dependencies (required)
ultralytics>=8.0
PyQt6>=6.0
opencv-python>=4.6.0
numpy>=1.20.0
pyyaml>=5.3.1

# Optional: SAM 2 segmentation  →  pip install yolo26-app[sam]
# sam2

# Optional: Grounding DINO zero-shot detection  →  pip install yolo26-app[dino]
# groundingdino-pip

# Optional: Intel RealSense depth camera  →  pip install yolo26-app[realsense]
# pyrealsense2>=2.50.0
